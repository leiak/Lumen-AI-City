[project]
name = "federation-id"
version = "0.1.0"
description = "联邦 FUUID - 跨城 NPC 唯一标识符（§E.5）"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
