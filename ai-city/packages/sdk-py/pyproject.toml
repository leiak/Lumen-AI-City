[project]
name = "aicity-sdk"
version = "0.1.0"
description = "AI City 第三方 Agent Python SDK（§20.17）"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.9",
    "cryptography>=43",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
